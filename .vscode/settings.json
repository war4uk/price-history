{
    "files.exclude": {
        "**/*.map": {},
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*.css": {
            "when": "$(basename).scss"
        }
    },
    
    "editor.tabSize": 4,
    "editor.insertSpaces": true
}